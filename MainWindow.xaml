<Window x:Class="ProcurementSystem.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:views="clr-namespace:ProcurementSystem.Wpf.Views"
        Title="Procurement System" 
        Width="900" 
        Height="700"
        Background="#FF1E1E1E">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <SolidColorBrush x:Key="DarkBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="DarkSurface" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="DarkBorder" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="AccentHover" Color="#FF1C97EA"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFF1F1F1"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFB0B0B0"/>

        <Style TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="{StaticResource DarkBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Border"
                                Background="Transparent"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                CornerRadius="0"
                                Margin="0,0,4,0">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkSurface}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF252526"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource DarkBackground}">
        <Border Background="{StaticResource DarkBackground}"
                BorderBrush="{StaticResource DarkBorder}"
                BorderThickness="1"
                CornerRadius="0">
            <TabControl x:Name="TabControl">
                <TabItem Header="👑 Користувачі" 
                         Visibility="{Binding AdminTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
                            Padding="16">
                        <views:AdminUsersView/>
                    </Border>
                </TabItem>
                <TabItem Header="📝 Звітність" 
         Visibility="{Binding AdminTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
            Padding="16">
                        <views:AdminReportsView/>
                    </Border>
                </TabItem>


                <TabItem Header="📝 Мої заявки" 
                         Visibility="{Binding EmployeeTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
                            Padding="16">
                        <views:EmployeeRequestsView/>
                    </Border>
                </TabItem>

                <TabItem Header="⏳ На погодженні" 
                         Visibility="{Binding ManagerTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
                            Padding="16">
                        <views:ManagerApprovalView/>
                    </Border>
                </TabItem>

                <TabItem Header="📥 Нові заявки" 
                         Visibility="{Binding PurchaserTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
                            Padding="16">
                        <views:PurchaserOrdersView/>
                    </Border>
                </TabItem>

                <TabItem Header="📋 Підтверджені замовлення" 
                         Visibility="{Binding PurchaserTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
                            Padding="16">
                        <views:PurchaserMyOrdersView/>
                    </Border>
                </TabItem>

                <TabItem Header="📋 Логи" 
                         Visibility="{Binding LogsTabVisibility}">
                    <Border Background="{StaticResource DarkSurface}" 
                            Padding="16">
                        <views:AuditLogView/>
                    </Border>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</Window>